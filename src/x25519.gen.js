/* Untyped file generated from x25519.ml by genType. */
/* eslint-disable */

import {default as loadNotChecked} from 'wasm-x25519';

import {diffie_hellman as diffie_hellmanNotChecked} from 'wasm-x25519';

import {derive_public as derive_publicNotChecked} from 'wasm-x25519';

import {generate_keypair as generate_keypairNotChecked} from 'wasm-x25519';

import {base64_encode as base64_encodeNotChecked} from 'wasm-x25519';

import {base64_decode as base64_decodeNotChecked} from 'wasm-x25519';

// In case of type error, check the type of 'load' in 'x25519.re' and 'wasm-x25519'.
export const loadTypeChecked = loadNotChecked;

// Export 'load' early to allow circular import from the '.bs.js' file.
export const load = loadTypeChecked;

// In case of type error, check the type of 'diffie_hellman' in 'x25519.re' and 'wasm-x25519'.
export const diffie_hellmanTypeChecked = diffie_hellmanNotChecked;

// Export 'diffie_hellman' early to allow circular import from the '.bs.js' file.
export const diffie_hellman = diffie_hellmanTypeChecked;

// In case of type error, check the type of 'derive_public' in 'x25519.re' and 'wasm-x25519'.
export const derive_publicTypeChecked = derive_publicNotChecked;

// Export 'derive_public' early to allow circular import from the '.bs.js' file.
export const derive_public = derive_publicTypeChecked;

// In case of type error, check the type of 'generate_keypair' in 'x25519.re' and 'wasm-x25519'.
export const generate_keypairTypeChecked = generate_keypairNotChecked;

// Export 'generate_keypair' early to allow circular import from the '.bs.js' file.
export const generate_keypair = generate_keypairTypeChecked;

// In case of type error, check the type of 'base64_encode' in 'x25519.re' and 'wasm-x25519'.
export const base64_encodeTypeChecked = base64_encodeNotChecked;

// Export 'base64_encode' early to allow circular import from the '.bs.js' file.
export const base64_encode = base64_encodeTypeChecked;

// In case of type error, check the type of 'base64_decode' in 'x25519.re' and 'wasm-x25519'.
export const base64_decodeTypeChecked = base64_decodeNotChecked;

// Export 'base64_decode' early to allow circular import from the '.bs.js' file.
export const base64_decode = base64_decodeTypeChecked;
